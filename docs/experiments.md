# Experiments

This document will track experiments performed using the robolog training
pipeline. Each experiment should log configuration, results, and any notes.

Use `training/train_ppo.py` with different configurations in
`configs/training.yaml` and capture outputs stored under `artifacts/`.
